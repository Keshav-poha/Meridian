# MERIDIAN Flutter app

The Flutter client is a presentation and device-adapter target; training and filtering semantics remain in `shared/`. The Stage 4 replay/live bridge will emit the `TelemetrySnapshot` model below so Developer Mode displays actual pipeline data rather than UI placeholders.

Run after installing Flutter:

```powershell
flutter pub get
flutter run
```
